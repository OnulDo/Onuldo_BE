spring:
  application:
    name: umc

  #  config:
  #    import: "file:./.env[.properties]"

  datasource:
    url: jdbc:mysql://localhost:3306/parkouroot
    username: parkouroot-user
    password: vkzmfnxm1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  token:
    secretKey: "local-development-jwt-secret-key-32-characters"
    refreshKey: ${JWT_TOKEN_EXPIRATION_REFRESH}
    expiration:
      access: 1800000 # 30분
      refresh: 1209600000 # 14일
vote:
  deadline-seconds: ${VOTE_DEADLINE_SECONDS:120}
